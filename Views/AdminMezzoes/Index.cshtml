@model IEnumerable<Young_snakes.Models.Mezzo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Veicolo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Veicolo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMezzo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMezzo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMezzo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
