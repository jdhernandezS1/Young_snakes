@model IEnumerable<Young_snakes.Models.Sponsor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SponsorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LogoUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTeam)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SponsorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LogoUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTeam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSponsor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSponsor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSponsor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
