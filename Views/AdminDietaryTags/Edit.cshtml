@model Young_snakes.Models.DietaryTag

@{
    ViewData["Title"] = "Edit";
}


<h4>Alergeni e Diete</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdTag" />
            <div class="form-group">
                <label asp-for="TagName" class="control-label">DIETA</label>
                <input asp-for="TagName" class="form-control" />
                <span asp-validation-for="TagName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TagType" class="control-label">TIPO</label>
                <input asp-for="TagType" class="form-control" />
                <span asp-validation-for="TagType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Torna a la lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
