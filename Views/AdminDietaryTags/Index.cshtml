@model IEnumerable<Young_snakes.Models.DietaryTag>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TagName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TagType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TagName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TagType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTag">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTag">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTag">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
